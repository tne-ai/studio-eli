Ensure you are using the TNE SDK save files. Here is an example:

from tne.TNE import TNE
session = TNE(uid=UID, bucket_name=BUCKET, project=PROJECT, version=VERSION)
session.upload_object({filename goes here},{data goes here})
